{
  "name": "@heliks/result",
  "version": "0.0.1",
  "description": "",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "author": "Felix Wolf",
  "license": "ISC",
  "scripts": {
    "build": "tsc",
    "build:prod": "npm run build",
    "lint": "eslint 'src/**' --config .eslintrc.json --ignore-path .eslintignore",
    "postinstall": "npm run build",
    "test": "jest --config jest.config.json",
    "watch": "tsc -w"
  },
  "devDependencies": {
    "@types/jest": "^24.0.6",
    "eslint-config-heliks": "git+ssh://git@github.com/Heliks/eslint-config.git",
    "jest": "^24.1.0",
    "ts-jest": "^24.0.0",
    "typescript": "^3.5.3"
  }
}
